{
    "default": {
        "train_batch_size": 4,
        "max_train_epochs": 28,
        "save_every_n_epochs": 4
    },
    "size_ranges": [
        {
            "min": 1,
            "max": 15,
            "config": {
                "train_batch_size": 4,
                "max_train_epochs": 40,
                "save_every_n_epochs": 20
            }
        },
        {
            "min": 16,
            "max": 35,
            "config": {
                "train_batch_size": 4,
                "max_train_epochs": 30,
                "save_every_n_epochs": 10
            }
        },
        {
            "min": 36,
            "max": 60,
            "config": {
                "train_batch_size": 4,
                "max_train_epochs": 25,
                "save_every_n_epochs": 5
            }
        }
    ]
}