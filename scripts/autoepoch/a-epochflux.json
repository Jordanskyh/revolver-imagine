{
    "default": {
        "train_batch_size": 1,
        "max_train_epochs": 10,
        "save_every_n_epochs": 2
    },
    "size_ranges": [
        {
            "min": 1,
            "max": 15,
            "config": {
                "max_train_epochs": 25,
                "save_every_n_epochs": 10
            }
        },
        {
            "min": 16,
            "max": 35,
            "config": {
                "max_train_epochs": 10,
                "save_every_n_epochs": 5
            }
        },
        {
            "min": 36,
            "max": 65,
            "config": {
                "max_train_epochs": 5,
                "save_every_n_epochs": 2
            }
        }
    ]
}