{
    "default": {
        "train_batch_size": 1,
        "max_train_epochs": 15,
        "save_every_n_epochs": 5
    },
    "size_ranges": [
        {
            "min": 1,
            "max": 15,
            "config": {
                "train_batch_size": 1,
                "max_train_epochs": 15,
                "save_every_n_epochs": 5
            }
        },
        {
            "min": 16,
            "max": 35,
            "config": {
                "train_batch_size": 1,
                "max_train_epochs": 10,
                "save_every_n_epochs": 5
            }
        },
        {
            "min": 36,
            "max": 60,
            "config": {
                "train_batch_size": 1,
                "max_train_epochs": 8,
                "save_every_n_epochs": 4
            }
        }
    ]
}