{
    "default": {
        "train_batch_size": 1,
        "max_train_epochs": 10,
        "save_every_n_epochs": 2
    },
    "size_ranges": [
        {
            "min": 1,
            "max": 15,
            "config": {
                "max_train_epochs": 25,
                "save_every_n_epochs": 10
            }
        },
        {
            "min": 16,
            "max": 35,
            "config": {
                "max_train_epochs": 12,
                "save_every_n_epochs": 4
            }
        },
        {
            "min": 36,
            "max": 100,
            "config": {
                "max_train_epochs": 6,
                "save_every_n_epochs": 2
            }
        }
    ]
}