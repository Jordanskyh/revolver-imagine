{
    "default": {
        "train_batch_size": 1,
        "max_train_epochs": 100,
        "save_every_n_epochs": 20,
        "optimizer_type": "prodigy",
        "optimizer_args": [
            "decouple=True",
            "d_coef=1.0",
            "weight_decay=0.01"
        ],
        "rank": 32,
        "alpha": 32,
        "conv_dim": 32,
        "conv_alpha": 32
    },
    "size_ranges": [
        {
            "min": 1,
            "max": 20,
            "config": {
                "steps": 1100,
                "save_every": 275
            }
        },
        {
            "min": 21,
            "max": 40,
            "config": {
                "steps": 1200,
                "save_every": 300
            }
        },
        {
            "min": 41,
            "max": 100,
            "config": {
                "max_train_epochs": 100,
                "save_every_n_epochs": 20
            }
        }
    ]
}