{
    "default": {
        "train_batch_size": 1,
        "max_train_epochs": 80,
        "save_every_n_epochs": 20
    },
    "size_ranges": [
        {
            "min": 1,
            "max": 15,
            "config": {
                "max_train_epochs": 200,
                "save_every_n_epochs": 50
            }
        },
        {
            "min": 16,
            "max": 35,
            "config": {
                "max_train_epochs": 80,
                "save_every_n_epochs": 20
            }
        },
        {
            "min": 36,
            "max": 65,
            "config": {
                "max_train_epochs": 40,
                "save_every_n_epochs": 10
            }
        }
    ]
}