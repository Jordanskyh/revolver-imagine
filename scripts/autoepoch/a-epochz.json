{
    "default": {
        "train_batch_size": 1,
        "max_train_epochs": 100,
        "save_every_n_epochs": 20
    },
    "size_ranges": [
        {
            "min": 6,
            "max": 15,
            "config": {
                "train_batch_size": 1,
                "max_train_epochs": 150,
                "save_every_n_epochs": 30
            }
        },
        {
            "min": 16,
            "max": 35,
            "config": {
                "train_batch_size": 1,
                "max_train_epochs": 100,
                "save_every_n_epochs": 20
            }
        },
        {
            "min": 36,
            "max": 60,
            "config": {
                "train_batch_size": 2,
                "max_train_epochs": 80,
                "save_every_n_epochs": 20
            }
        }
    ]
}