{
    "default": {
        "train_batch_size": 4,
        "max_train_epochs": 50,
        "save_every_n_epochs": 10
    },
    "size_ranges": [
        {
            "min": 1,
            "max": 15,
            "config": {
                "train_batch_size": 4,
                "max_train_epochs": 120,
                "save_every_n_epochs": 30
            }
        },
        {
            "min": 16,
            "max": 35,
            "config": {
                "train_batch_size": 8,
                "max_train_epochs": 80,
                "save_every_n_epochs": 20
            }
        },
        {
            "min": 36,
            "max": 60,
            "config": {
                "train_batch_size": 16,
                "max_train_epochs": 50,
                "save_every_n_epochs": 10
            }
        }
    ]
}