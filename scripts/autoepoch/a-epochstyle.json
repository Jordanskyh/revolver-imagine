{
    "default": {
        "train_batch_size": 4,
        "max_train_epochs": 50,
        "save_every_n_epochs": 10
    },
    "size_ranges": [
        {
            "min": 6,
            "max": 15,
            "config": {
                "train_batch_size": 2,
                "max_train_epochs": 100,
                "save_every_n_epochs": 25
            }
        },
        {
            "min": 16,
            "max": 35,
            "config": {
                "train_batch_size": 4,
                "max_train_epochs": 70,
                "save_every_n_epochs": 20
            }
        },
        {
            "min": 36,
            "max": 60,
            "config": {
                "train_batch_size": 8,
                "max_train_epochs": 50,
                "save_every_n_epochs": 10
            }
        }
    ]
}